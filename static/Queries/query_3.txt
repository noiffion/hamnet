SELECT theatres.id, theatres.theatre_name, cities.id as city_id,
       cities.city_name, theatres.address, theatres.webpage, theatres.user_id 
FROM theatres JOIN cities
ON theatres.city_id = cities.id
WHERE (theatres.user_id = {0}) OR (theatres.user_id IS NULL)
ORDER BY theatres.theatre_name ASC;

/*
SELECT theatres.id, theatres.theatre_name, cities.id as city_id, cities.city_name, theatres.address, theatres.webpage, theatres.user_id FROM theatres JOIN cities ON theatres.city_id = cities.id WHERE (theatres.user_id = {0}) OR (theatres.user_id IS NULL) ORDER BY theatres.theatre_name ASC;
*/
